# Rules for Content Creation
- When creating or updating page content, **DO NOT** use hyphens (`-`), en dashes (`–`), or em dashes (`—`) in any of the visible text. Always use spaces instead of dashes to ensure consistency across the application.
